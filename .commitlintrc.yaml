---
extends:
  - "@commitlint/config-conventional"
rules:
  header-max-length:
    - 0
  body-max-line-length:
    - 0
  type-enum:
    - 2
    - always
    - - build
      - ui
      - ci
      - docs
      - feat
      - fix
      - perf
      - refactor
      - revert
      - format
      - test
      - chore
